import { Request, Response } from "express";

export const getProducts = async (req: Request, res: Response) => {
  try {
  } catch (error) {
    console.log(error, "error while getProducts");
  }
};
